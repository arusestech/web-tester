# 우리은행 VOC — 커밋 주제별 시나리오

브랜치 `feature/org-succ-lineage` 에 쌓인 커밋을 주제별로 검증한다.
폴더 하나 = 커밋 1건(또는 한 주제). 공통 설정은 `_공통.json` 하나에 모으고 각 폴더는 `"extends": "../_공통.json"` 만 쓴다.

소스: `D:/source/IdeaProjects/mr-voc-wrb_voc_current`

## 폴더 ↔ 커밋

| 폴더 | 커밋 | 주제 |
|---|---|---|
| `개선7_통합검색` | `dd82b030e7` + `3eaf506ce4` | 검색항목 14종·1단계 선택 조회, 귀책대상 하위포함 |
| `결재_임의종결_중복상신` | `e305a2cb38` | 운영자 결재함 전체조회·임의종결(CPACLOSE)·중복상신 차단(결함34·44) |
| `결재_회수정책` | `64921b71b6` | CPA 12개 모달 선행 결재 가드 제거 |
| `결함9_유형정보` | `71357c2229` | 유형정보 라디오 값 관리코드 환원 |
| `개선22_공통오류` | `f5b8e25e1c` | 추적번호(traceId)·공통 오류 페이지 |
| `결함49_권한공통코드` | `33de44d4c1` | 부점코드 하드코딩 → 공통코드(WRBAUTH) |
| `결함50_제도개선포켓` | `7138e704d9` | 중복행 방지·ORA-01427/00933 대응 |
| `그룹사원변경이력` | `5cfa9d6b3f` | 기준일자 시점 구성원 모달·엑셀 |
| `마일리지_기준일자` | `833999ca6c` | 결함30·48 업로드 처리·STANDARD_YMD |
| `보안_기동점검` | `81ddb4fb3c`, `72fd20da39` | actuator 제거(CVE-2026-40984), FINES 미기동 대응 |

시나리오를 만들지 않은 커밋 — `1bb1e60de0`(문서), `dbe11f28d2`(DDL), `d8a5db9abe`(prod yml),
`b57ddeba52`(AI Agent, 연동 주소 미확정), `3163e9794e`(API — JWT 필요, 화면 경유 아님),
`34fd2dd058`(엑셀 경과일 — 다운로드가 이력을 남겨 `개선7_통합검색/_05` 로 분리해 둠).

## 검증 깊이 (솔직하게)

| 깊이 | 어디에 |
|---|---|
| **동작까지 검증** | 개선7(검색항목 렌더·조건별 기대건수), 결함9(라디오 value 가 관리코드인지), 개선22(오류 페이지 요소), 보안(actuator 응답), 그룹사원변경이력(모달 요소) |
| **로드·무오류까지** | 결재 회수정책, 결함50, 결함49, 마일리지 |

로드까지만 보는 폴더도 `_공통.json` 의 `errorStatus`(486/487/488)와 `errorPatterns`(ORA-xxxxx)가
걸려 있어 **매퍼 오류·업무오류는 잡힌다.** 다만 "버튼을 눌렀을 때 의도대로 도는가"는 못 본다 —
그 부분은 대부분 결재 상신·승인·엑셀 업로드처럼 프로젝트가 금지한 동작이라 자동화 대상에서 뺐다.

## 실행

```
run.bat scenarios\우리은행VOC                          (전체 일괄)
run.bat scenarios\우리은행VOC\결함9_유형정보           (폴더 하나)
run.bat lint scenarios\우리은행VOC
```

- 서버는 18081 로 띄운다(사용자 IntelliJ 8081 과 분리). 기동 방법은 프로젝트 `CLAUDE.md`.
- 비밀번호는 `{{password}}` 로 실행할 때 입력받는다. 계정 `b00756`.

## 전제 — 테스트 데이터

`결함9_유형정보` 와 `개선7_통합검색/04` 는 상세 모달·건수 판정에 업무 데이터가 필요하다.

```
DB반영/20_개선7_통합검색_테스트데이터_260829.sql    VOC 6건(TESTVOC001~006)
DB반영/20b_개선7_FINES참조번호_테스트데이터_260829.sql
DB반영/21_결함44_중복상신가드_테스트데이터_260829.sql
```

CCCOWN 에는 적용돼 있다. **검증이 끝나기 전에는 지우지 않는다.**

## 환경 기지 이슈

`_공통.json` 에서 무시 처리한 것 — `/addMenuLog` 488(FN_VOC_AUTO_SEQ INVALID, 매 화면 반복),
`parent.customConfirm`(화면 URL 직접 접근 시 부수 현상).

2026-08-29 에 `SYS_APRV_LINE_BAS`·`SYS_APRV_LINE_DTL` 을 복원해 결재 모달과
결재라인 관리 2화면의 488 은 해소됐다(`DB반영/22_결재라인테이블_누락복원_260829.sql`).
