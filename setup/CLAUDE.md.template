# {{프로젝트명}}

> 이 파일을 프로젝트 루트에 `CLAUDE.md` 로 복사하고 `{{ }}` 부분을 채운다.
> 비밀번호 같은 민감정보는 git에 커밋하지 말 것 (`.gitignore`에 CLAUDE.md 추가하거나 `CLAUDE.local.md` 사용).

## 개요
- 기술: Java {{버전}} / {{Spring MVC | 순수 JSP+Servlet | 전자정부 등}} / JSP / {{DB}}
- 빌드: {{Maven | Gradle | Ant | 이클립스 WAR}}
- WAS: {{Tomcat 9 등}}

## 기동 / 접속
- 기동: {{`mvnw spring-boot:run` | `D:\tomcat\bin\startup.bat` | 인텔리제이 Run 구성 "{{이름}}"}}
- 접속 URL: http://localhost:{{8080}}/{{context}}
- 기동 확인: {{URL}} 응답 200 + 로그인 화면 표시

## 테스트 계정
- 관리자: `{{admin}}` / `{{비번}}`
- 일반사용자: `{{user1}}` / `{{비번}}`
- 로그인 화면: {{/login.jsp}} — 아이디 input `{{#userId}}`, 비번 input `{{#userPw}}`, 로그인 버튼 `{{#btnLogin}}`

## 메뉴 구조
<!-- 없으면 Claude에게 "소스에서 메뉴 목록 추출해서 여기 채워줘" 라고 시킬 것 -->
| 대메뉴 | 소메뉴 | URL | 주요 기능 | 비고 |
|---|---|---|---|---|
| {{사원관리}} | {{사원목록}} | {{/hr/empList.do}} | 검색, 등록, 수정, 삭제 | |
| | {{부서관리}} | {{/hr/deptList.do}} | | 트리 UI |
| {{급여}} | {{급여조회}} | {{/pay/list.do}} | 월 선택 조회, 엑셀 | |

## 화면 특성
- {{iframe 구조: 좌측 메뉴 `#leftFrame`, 본문 `#mainFrame`}}
- {{팝업: 우편번호/상세조회는 window.open}}
- {{공통 alert: 저장 시 "저장되었습니다"}}

## 정상 판정 기준
- HTTP 5xx / 톰캣 에러 페이지 없음
- 콘솔 JS 에러 없음
- 목록 화면은 테이블 헤더 + 1건 이상 데이터 표시 (데이터 없으면 "데이터가 없습니다" 문구)
- {{추가 기준}}

## 절대 하지 말 것
- 실데이터 삭제 (테스트 데이터 `TEST_` 접두어만 허용)
- {{메일/SMS 발송, 결재 상신, 마감 처리, 배치 실행 버튼}}
- {{운영 DB 접속 설정 변경}}

## 테스트 데이터
- 등록 테스트 시 이름/제목은 `TEST_` 접두어 사용, 테스트 후 직접 삭제
- {{기타}}
